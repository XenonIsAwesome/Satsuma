{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the borderless trigger overlay",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
